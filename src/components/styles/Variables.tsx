import styled, { css } from "styled-components";
export const btnReset = css`
  font-family: inherit;
  outline: none;
  border: none;
  background: none;
  letter-spacing: inherit;
  color: inherit;
  font-size : inherit;
  text-align: inherit
  padding: 0;
`;
